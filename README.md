# dotfiles

### Install

```bash
$ git clone https://github.com/BastienM/dotfiles.git  $HOME/.dotfiles
$ cd $HOME/.dotfiles
$ mkdir -p $HOME/.config/dotdrop && ln -s config/dotdrop/config.yaml $HOME/.config/dotdrop/config.yaml
$ dotdrop install --profile (work|home)
```
